# Ski Rental with Distributional Predictions of Unknown Quality

OpenReview `h8NydU966G` · arXiv `2602.21104v1`.

This will be a clean-room, exact-rational reproduction because the primary
source does not provide author code. It covers all three live claims: the
distribution-sensitive upper bound, its consistency/robustness implications,
and the rule's independence from an unknown prediction-error bound.

The verifier will exhaust rational finite distributions, reconstruct the
source threshold rule, test its transported-tail proof chain, and evaluate the
source hard family across multiple buy-cost scales. No reduced-scale learning
or sampled proxy is needed.
